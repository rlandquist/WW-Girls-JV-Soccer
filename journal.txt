=== Journal Entry 2026-04-26T19:18:53.767878+00:00 ===
2026-04-26-19-18-52-ww-soccer-github-retrofit.txt
[Multi-session technical conversation retrofitting three HTML-based soccer card builder tools (GirlsJVSoccerCard.html, GirlsJVSoccerSchedule.html, GirlsJVSoccerRoster.html) to use GitHub as canonical storage instead of File System Access API, with PWA support for mobile use. Contains: project background review, mobile/PWA architectural decisions, common.js shared library creation (already committed), and in-progress retrofit of the card tool with detailed code edits via str_replace operations.]




=== Journal Entry 2026-04-26T19:34:13.724022+00:00 ===
2026-04-26-19-34-12-ww-soccer-card-retrofit.txt
[Multi-session technical conversation retrofitting three HTML soccer card builder tools (GirlsJVSoccerCard.html, GirlsJVSoccerSchedule.html, GirlsJVSoccerRoster.html) at github.com/rlandquist/WW-Girls-JV-Soccer to use GitHub REST API as canonical storage instead of File System Access API, with PWA support for mobile use. Contains: project background, mobile/PWA architectural decisions, common.js shared library (already committed), and substantial in-progress retrofit of the card tool with detailed code edits via str_replace operations.]




=== Journal Entry 2026-04-26T19:48:19.522670+00:00 ===
2026-04-26-19-48-18-ww-soccer-schedule-retrofit.txt
[Multi-session technical conversation retrofitting WW Girls JV Soccer HTML tools to use GitHub REST API as canonical storage instead of File System Access API. Card tool retrofit COMPLETED and delivered. Schedule tool retrofit IN PROGRESS at the moment of compaction. Contains: project background, completed common.js library details, completed card tool changes, file landmarks for the schedule tool, and the retrofit plan for both schedule and roster tools.]




=== Journal Entry 2026-04-26T19:56:05.347880+00:00 ===
2026-04-26-19-56-04-ww-soccer-roster-retrofit.txt
[Multi-session technical conversation retrofitting WW Girls JV Soccer HTML tools to use GitHub REST API as canonical storage instead of File System Access API. Card tool and Schedule tool retrofits COMPLETED and delivered. Roster tool retrofit IN PROGRESS at the moment of compaction. Contains: project background, completed common.js library, completed card+schedule tool changes, file landmarks for the roster tool, and the retrofit plan.]




=== Journal Entry 2026-04-26T20:14:55.376771+00:00 ===
2026-04-26-20-14-54-ww-soccer-pwa-retrofit.txt
[Multi-session technical conversation retrofitting WW Girls JV Soccer HTML tools to use GitHub REST API as canonical storage instead of File System Access API. All three tool retrofits (Card, Schedule, Roster), shared common.js library, landing page (index.html), and Web App Manifest are COMPLETE. Service worker (sw.js) is BUILT but not yet integrated. Contains: project background, completed deliverables with their final paths, file landmarks, retrofit plan, and remaining tasks (HTML PWA wiring + README).]



=== Journal Entry 2026-04-26T20:30:00.000000+00:00 ===
2026-04-26-20-14-54-ww-soccer-pwa-retrofit.txt
[FINAL SESSION — PROJECT COMPLETE. All eight deliverables shipped to /mnt/user-data/outputs/: index.html (landing page, 15KB, varsity-poster aesthetic, Oswald display font, three tool tiles with color-coded accent strips, mounts shared GH config panel), GirlsJVSoccerCard.html (157KB, retrofitted + PWA-wired), GirlsJVSoccerSchedule.html (62KB, retrofitted + PWA-wired), GirlsJVSoccerRoster.html (69KB, retrofitted + PWA-wired), common.js (40KB shared GitHub-sync library), manifest.json (PWA manifest with 4 icons + 3 jump-shortcuts for Card/Schedule/Roster, theme-color #0d1f42, standalone display, portrait orientation, scope ./, id /WW-Girls-JV-Soccer/), sw.js (8KB service worker; cache-first with stale-while-revalidate for app shell, cache-on-demand for opponent logos in /Logos/, cross-origin pass-through for api.github.com/fonts/cdnjs, navigation fallback to cached index, postMessage SKIP_WAITING handler for future update UI, CACHE_VERSION='v1'), README.md (13KB / 297 lines, covers PAT generation walkthrough with exact GitHub UI breadcrumbs and direct link, per-device install instructions for iOS Safari and Android Chrome with per-page home-screen title table, daily use, conflict handling, opponent logo CamelCase + case-sensitivity convention, offline behavior, five-mode troubleshooting with concrete fixes, developer notes including CACHE_VERSION bump rule and full JSON schemas for teams.json/schedule.json/roster.json, file inventory table). All HTML well-formed (HTMLParser validation passed for all four files), all inline JS parses with node --check. Schedule tool ended at 61KB/1649 lines from original 145KB/1841. Roster tool ended at 68KB/1799 lines from original 151KB/1973. Per-page apple-mobile-web-app-title set so tools can be installed separately on home screens: WW Soccer (landing) / Soccer Card / Schedule / Roster. Year references removed from landing page per user feedback mid-session; "Coach Tools" renamed to "Social Media Tools" so the page reads year-agnostic for multi-year reuse. Total project size ~366KB across 8 files (smaller than original 3 tools at 536KB combined, due to extracting the 70KB base64 paw mark out of each HTML and stripping the FSA stack). Deployment: git add the eight files to repo root, commit, push to main, wait 1-2 min for Pages rebuild, then first device-setup step in README. Repo layout when deployed: index.html + 3 tool HTMLs + common.js + manifest.json + sw.js + teams.json + schedule.json + roster.json + Logos/ at root. NO REMAINING TASKS — entire architecture migration from File System Access API to GitHub REST API as canonical storage, plus PWA + landing page + docs, is complete and ready for Rob to deploy.]



=== Journal Entry 2026-04-26T21:00:00.000000+00:00 ===
2026-04-26-21-00-00-ww-soccer-schedule-custom-logo.txt
[Short follow-up session on the WW Girls JV Soccer tools (already-deployed
GitHub-API-canonical architecture). One surgical edit to GirlsJVSoccerSchedule.html's
logoFor() function: previously returned null when a typed opponent name didn't match
any team in the OPPONENTS list, so custom-typed opponents rendered with no logo.
Updated to fall back to a derived filename — strip whitespace from the typed name,
preserve case, append .png — so a typed "Kettle Moraine" now resolves to
./Logos/KettleMoraine.png even when not added to the managed teams list. OPPONENTS
matches still win (their explicit `file` field is honored first); only unmanaged
custom names use the derived path. Empty/whitespace-only names still return null.
Filenames remain case-sensitive on GitHub Pages per existing convention. No other
files touched. README's "Opponent logos" section already documents the CamelCase
naming convention so the new fallback aligns with documented behavior — no doc
update needed.]



=== Journal Entry 2026-05-02T16:55:00.000000+00:00 ===
2026-05-02-16-55-00-ww-soccer-card-scorer-dropdown-first-name.txt
[Short follow-up session on the WW Girls JV Soccer tools (already-deployed
GitHub-API-canonical architecture). One surgical edit to GirlsJVSoccerCard.html's
renderScorersPicker() function: dropdown labels for roster players previously
showed "#NN LAST" only (e.g. "#10 Johnson"), which made disambiguation hard
mid-game when two players share a surname. Updated to "#NN First Last"
(e.g. "#10 Sarah Johnson") with fallbacks — both names: "First Last"; only
last: "Last"; only first: "First"; neither: "(unnamed)". Change is editor-side
only; the on-card scorer line still goes through scorerDisplayName(), which
remains last-name-only uppercased ("#10 JOHNSON · 2") to match sports-card
convention. Because renderScorersPicker() builds BOTH the Final-card and
Halftime-card pickers from the same code path, the change applies to both
pickers automatically with no fork — confirmed with the user before editing.
CSS and HTML structure untouched; existing escapeHtml/escapeAttr handling on
the label string is unchanged. Validated: HTMLParser passes, node --check on
extracted inline JS passes, diff vs uploaded original is exactly the intended
block (one two-line label assignment expanded to ten lines — three trimmed-name
locals, four-clause nameLabel ternary, final label assembly with comment
explaining the editor-only scope of the change). Service worker CACHE_VERSION
not bumped — change is small enough that stale-while-revalidate next-reload
pickup is acceptable; bump to v2 only if forced first-reload pickup is wanted.
No other files touched. Process note for future sessions: file retrieval got
stuck early when reproducing the 157KB HTML from in-context document content
was attempted via create_file; user re-uploaded the file directly to
/mnt/user-data/uploads, which unblocked the edit immediately. For large
existing files in this project, prefer direct upload over context reproduction.]

=== Journal Entry 2026-05-02T21:30:00.000000+00:00 ===
2026-05-02-21-30-00-ww-soccer-conferences-and-back-link.txt
[Long follow-up session on the WW Girls JV Soccer tools (already-deployed
GitHub-API-canonical architecture). Two features shipped: conference-badge
support across Card + Schedule + teams.json schema, and a "← Main"
back-link at the top of the editor sidebar in all three tools.

Conference badge feature: teams.json schema bumped to v2 — new top-level
`conferences` map of { id: { name, logo } } plus a per-team `conference`
field that's null by default. Initial commit ships with a single
`classic-8` entry pointing at Logos/Classic8.png and every team's
`conference` set to null per Rob's request — assignments to be flipped
via the UI rather than guessed in advance. Card tool got 15 surgical
edits: CSS for the .conf-badge overlay (22×22 white circle, bottom-right
corner of the opponent's logo box, drop-shadow, 80% inner sizing); a
CONFERENCES global plus conference-aware paths through parseStoredPayload,
buildStoredPayload, loadOpponentsFromStorage, applyTeamsPayload, and
importTeamsJson so the conferences map and per-team `conference` field
round-trip cleanly through both localStorage and the GitHub PUT path;
two helpers conferenceLogoFor(oppName) and confBadgeHTML(oppN); badge
appended in setLogoOppScore/Preview/Halftime in both image-loaded and
fallback-initial branches; team manager modal grid widened from 3-col
to 4-col with a new C8 checkbox column. Short label derived from the
conference id ('classic-8' → 'C8') so adding a second conference later
doesn't require renaming. .team-logo / .vs-logo-box rules got
`position:relative` (was missing) so the badge anchors correctly.
Custom-typed opponents (not in OPPONENTS) always return null from
conferenceLogoFor — only managed teams can carry the flag.

Schedule tool got 6 surgical edits: an inline .gconf badge next to
opponent names in the row layout (14×14 — smaller than the cards' 22px
since rows are dense — 13px text, 24px team logos), CONFERENCES global
declared before var OPPONENTS = loadOpponents() so the loader can
write into it, conference-aware paths through loadOpponents and
refreshTeamsFromGitHub, the same conferenceLogoFor helper, and
renderGameRow appending a confBadge variable inside .gopp so the
existing ellipsis truncation still works on long opponent names.
Initial Schedule .gconf had no white-circle backing (just a transparent
badge image floating on the navy row background) — Rob caught this
immediately and a follow-up edit aligned it with the cards' design:
white circle, border-radius 50%, 78% inner sizing, subtle box-shadow.

Back-link feature: small "← Main" pill at the very top of the editor
sidebar in all three tools, above the GitHub Sync panel. Card and
Schedule use the project navy (#0d1f42); Roster uses its own
royal-blue (#1e40af) to match its existing palette. Same markup across
all three: <a class="back-link" href="./" title="Back to main page">
<span class="arrow">←</span> Main</a>. href="./" works because
index.html sits at the same directory level as the tool HTMLs on Pages
— no path-juggling regardless of which subpath the user landed at.

Editing approach throughout: all changes via Python scripts with
bytes-mode read/write to preserve CRLF line endings exactly, and a
replace_once helper that aborts if an anchor doesn't appear exactly
once in the source. Card and Schedule are uniformly CRLF; Roster has
mixed CRLF+LF (121 bare LFs alongside ~1700 CRLFs) which was preserved
as-is rather than normalized. HTML well-formedness, line-ending counts,
and node --check syntax validation run on every output before delivery.
The conference field is purely additive — files without `conferences`
or per-team `conference` parse cleanly via the default-to-null fallback
in parseStoredPayload, so no migration concerns for the deployed state.

Final deliverables: GirlsJVSoccerCard.html (163KB, +5.5KB from
baseline), GirlsJVSoccerSchedule.html (66KB, +3KB),
GirlsJVSoccerRoster.html (70KB, +0.6KB), teams.json (v2 schema with
classic-8 entry and all teams' conference set to null). Remaining
tasks for Rob: drop Logos/Classic8.png into the repo (CamelCase,
case-sensitive on Pages), commit the four files plus the new logo,
then open the Card tool's Manage teams modal and flip the C8 checkbox
for each Classic 8 opponent. Schedule picks up the flags automatically
on next load. Roster got the back-link for navigation consistency but
no conference work — it doesn't display opponents.

Conversation-flow note worth recording: my first read of the repo
incorrectly concluded the April 26 retrofit hadn't deployed — the
GitHub web UI directory listing came back partial through web_fetch
(showed only a subset of root files), and I read the absence as
not-shipped. Rob corrected this; the rest of the session proceeded
against the actual deployed state. Future sessions should not
re-walk that ground — the architecture migration is live and stable.]

=== Journal Entry 2026-05-02T22:45:00.000000+00:00 ===
2026-05-02-22-45-00-ww-soccer-photo-credit-and-back-link.txt
[Continuing the same long session that landed conferences earlier today.
Two more features added: a Back to Main link at the top of the editor
sidebar in all three tools, and a Robert Landquist Photography credit
on all four pages (three tools + landing). Several rounds of iteration
on the photo credit before it landed cleanly — captured here so the
final state is reproducible from the journal alone.

Back-link feature: small "← Main" pill at the very top of the editor
sidebar, above the GitHub Sync panel. Same markup across all three
tools — `<a class="back-link" href="./" title="Back to main page">
<span class="arrow">←</span> Main</a>`. The href="./" works because
index.html sits at the same directory level as the tool HTMLs on
Pages — no path-juggling regardless of which subpath the user landed
at. Card and Schedule use the project navy (#0d1f42); Roster uses
its own royal blue (#1e40af) to match its existing palette. CSS rule
is identical except for that one color value. Card/Schedule edited
from local outputs (already had conference work applied); Roster was
uploaded fresh and edited from the upload. All three preserved their
original line endings — Card and Schedule uniformly CRLF, Roster
mixed CRLF+LF (121 bare LFs alongside ~1700 CRLFs), preserved as-is.

Photo credit feature: started with the wrong premise — Rob said the
provided logo PNG had a transparent background, but inspection showed
it was actually JPEG, RGB mode, with the black background being part
of the image. Pivoted the design to take advantage of that: a black
pill containing the logo + "© Robert Landquist Photography" text. The
dark container does double duty — it gives the credit a clear visual
identity AND it means the logo's black background blends into the
container regardless of whether someone later swaps in a transparent
PNG. Initial placement was at the bottom of the editor sidebar with
margin-top:auto pushing it to the bottom of the editor's flex column.
Rob asked for it in the center of the page instead, so for the three
tools it became `position:fixed; bottom:20px; left:50%;
transform:translateX(-50%); z-index:10` — pinned to the viewport
bottom-center alongside the existing #toast-container, which uses the
same fixed-positioning pattern. Index page kept the credit in document
flow at the page bottom (no editor/card horizontal split there, so a
fixed-position pill would feel out of place). On index, the existing
single-line `<p class="foot">Waukesha West Wolverines</p>` was
replaced with a structured footer that stacks the school tagline
above a black credit pill below. Old .foot and .foot .dot CSS rules
were left in the stylesheet as harmless dead code rather than churning
the file to remove them.

Logo iteration: this is where most of the back-and-forth happened.
First the logo file (originally a 1254×1254 square JPEG with the
camera+RL mark above "ROBERT LANDQUIST" text above "PHOTOGRAPHY"
tagline) was downscaled to 256×256 and dropped into the credit pill
at width:32px height:32px. Looked tiny. Bumped to 56×56. Rob noted
it looked stretched — turned out the file was square but the actual
logo CONTENT was only ~1112×773 (1.44:1 wider than tall) with massive
black padding above and below. Tried two fixes: cropping the source
image to the content bounds, and switching CSS to height-only with
width:auto so aspect ratio rendered naturally. That made the pill
~67×48 instead of 56×56. Rob came back with a screenshot showing it
STILL looked bad — the cropped logo had three vertically-stacked
elements (camera mark, "ROBERT LANDQUIST" text, "PHOTOGRAPHY" tag)
all squished into a 48px height, while the pill ALSO had readable
"© Robert Landquist Photography" text right next to it. The logo
was competing with itself: tiny illegible text inside the image,
duplicated by the readable text outside. Cropped to JUST the camera+RL
mark portion (rows 234..818 in the source), dropping both text blocks.
Bumped CSS height to 56px. Rob then provided a properly-prepared
icon-only logo file (782×647, 1.21:1, JPEG misnamed as .png) — used
that as the new source, converted to true PNG, resized to 512×424.
The HTMLs needed no further edits since the filename and aspect-ratio
CSS were unchanged.

Final state: Logos/RobertLandquistPhotography.png is the icon-only
mark at 512×424, ~121KB. CSS on all four pages renders it at
height:56px width:auto inside a black pill with white text at
font-size:13px, letter-spacing:0.05em. Pill positioning is
fixed-center on Card/Schedule/Roster, document-flow on index. Sizes
checked at the end: Card 164KB, Schedule 67KB, Roster 71KB,
index 17KB.

Editing approach throughout: same Python script + replace_once
pattern as the conferences work — bytes-mode read/write to preserve
line endings, abort on anchor mismatch. One Python gotcha bit me
midway: bytes literals can't contain non-ASCII, so a CSS comment
with em-dashes failed to compile. Fixed by writing as a string and
encoding to bytes at concat time. Worth remembering for future.

Conversation-flow note: I made an error of confidence early on with
the photo credit logo. I confirmed via PIL that the file was JPEG/RGB
without transparency, and explicitly told Rob he had three options
(re-export, alternative source, or threshold-key the black). I then
proceeded to design around option implicit-zero ("just put it on a
dark background") without him picking one. That actually turned out
fine because Rob hadn't expressed a preference, but it's worth being
more explicit next time about which option I'm taking when the user
hasn't picked. The aspect-ratio iteration was a real bug — I missed
that the source had THREE content blocks not one, and shipped a
"fix" (cropping to content bounds) that was insufficient. The right
diagnosis came from looking at Rob's screenshot, which made the
self-competing-text issue obvious. Worth remembering: when the user
sends a screenshot of the current state, study it carefully before
proposing the next fix.

Remaining tasks for Rob: deploy the five updated files (four HTMLs
plus the new logo PNG), then optionally bump CACHE_VERSION in sw.js
from 'v1' to 'v2' if force-refresh doesn't pick up the new logo on
already-cached devices.]

=== Journal Entry 2026-05-08T23:30:00.000000+00:00 ===
2026-05-08-19-06-47-ww-soccer-goals-tool-build.txt
[Multi-feature session on the WW Girls JV Soccer tools (already-deployed
GitHub-API-canonical architecture with PWA wrapping). New
GirlsJVSoccerGoals.html tool added end-to-end with cross-tool
integration. Built mobile-optimized goal tracker with horizontal
scrollable minute picker, sticky-half toggle, schedule+manual game
picker, and immediate save on every Add/Delete/Edit (no debounce — keeps
data durable against accidental closes). Surgical-edited the Card tool
to read scorers from goals.json instead of teams.json's scorersByGame,
with a new totals/times display toggle and a matching schedule+manual
game picker. Updated index.html and manifest.json to 4-tile order
(Card · Goals · Schedule · Roster) so Goals sits adjacent to Card in
the workflow. Bumped sw.js CACHE_VERSION from 'v1' to 'v2' so existing
PWA installs evict their v1 shell on next visit and pick up the new
Goals.html plus the modified Card.html.

Goals tool internals: goals.json schema is {version:1,
goalsByGame:{[gameKey]:[entry,...]}} where gameKey =
"YYYY-MM-DD|opp-lowercased" and each entry = {id, kind:
'roster'|'custom'|'unknown', playerId, customName, minute|null, half:
1|2}. New localStorage keys: 'ww-soccer-goals-v1' (data),
'ww-soccer-goals-prefs-v1' (half/view), 'ww-card-scorer-display-v1'
(Card tool's totals/times pref). Card tool also reads
'ww-soccer-schedule-v1' now for its new picker. Minute picker is a
121-cell strip (0–120, 56px wide each, scroll-snap-type:x mandatory +
scroll-snap-align:center, deep-red center indicator marks the active
cell). Three input surfaces: scroll/swipe (debounced 90ms listener
picks the cell closest to center after rest), tap a cell directly
(smooth scroll to it), or use the −/+ arrow buttons for ±1 nudges.
Buttons support hold-to-repeat: tap = 1, hold (>400ms) = 1 + 400ms
grace + auto-advance every 120ms (~8 min/sec) until release or
boundary. Implementation uses pointerdown/up/cancel/leave for
cross-platform mouse+touch+pen with a didRepeat flag so the trailing
click after a hold doesn't add a stray +1; keyboard Enter/Space
increments via the normal click handler. Default minute on game
selection = highest minute among existing goals (so reopening a game
in progress lands near where you left off), or 0 for a fresh game;
after each Add, lastEnteredMinute holds the just-added value as the
next entry's default (clusters at 12', 14', 23' need only minor
scrolling). One-shot migration on first Goals-tool boot expands legacy
teams.json scorersByGame entries (one row per game, {goals:N} count)
into N individual goal rows in goals.json (minute=null, half=1, since
neither was tracked retroactively), then saves a cleaned teams.json
without the scorersByGame field. Card tool's buildStoredPayload no
longer writes scorersByGame so the cleanup persists.

Phase 1 — Unknown scorer: third 'kind' value added to goal entries
alongside 'roster' and 'custom'. Unknown sits at the TOP of both
entry-form and edit-form scorer dropdowns ("— Unknown scorer —") since
live entry of "I saw a goal but missed who" needs one tap, not
scrolling past the roster. Renders italic-muted in the Goals tool list
and summary with a "TBD" tag, and is always-last sorted in the summary
so unattributed goals stay surfaced for follow-up. Card tool
aggregates all unknowns for one game into a single "UNKNOWN" bucket
(no jersey #) — placed last in totals mode, interleaved chronologically
by earliest minute in times mode. Edit-form dropdown supports switching
from 'unknown' → roster/custom for the fix-it-later workflow when the
tracker figures out who scored.

Phase 2 — Final Result section: lives in the Goals tool below the Game
Total, visible whenever a game is selected. Five render states keyed
off currentScheduleEntryId × entry status: no-game (hidden), manual-mode
(explainer linking to Schedule tool, no save UI), scheduled-not-final
(Mark-Final form), already-final (Update-Result + Un-finalize buttons,
drift hint), cancelled (read-only, points back to Schedule tool). WW
score is auto-derived from currentGoals().length and never editable
here (forces consistency with the goal list); opp score is a 0–99
number input pre-filled with saved scoreThem. Drift hint appears only
when the game is final AND saved scoreUs ≠ live goal count: "↻ saved
as N — re-save to update" (Option A: manual re-save with hint, chosen
over Option B auto-resync because cross-tool concurrent edits make
silent writes surprising). schedule.json write-back loads fresh,
applies only the targeted entry's status/scoreUs/scoreThem (preserves
version, layout, and every other game byte-for-byte), saves back.
SHA-conflict path re-applies the change against merged data and saves
once more — handles the "Schedule tool wrote between our load and our
save" race. Save button shows "Saving…" + disabled state during the
round-trip; toasts cover success ("Result saved to schedule ✓"),
local-only ("Saved locally — will sync when GitHub is configured"),
missing-entry ("Game no longer in schedule"), failure ("Save failed").
Un-finalize sets status='scheduled' but leaves scoreThem intact —
accidental un-finalize shouldn't erase typed values.

Card tool restructuring: both Final-card and Halftime-card scorer
editor tabs lost their picker UI entirely — replaced with a
.goals-link-block notice ("Scorers are managed in the Goals tool…")
+ "Open Goals tool ↗" button + status hint that color-states between
.empty ("No goals tracked yet for this game") and .loaded ("3 goals
tracked for this game ✓"). Final-card tab additionally got the
display-mode toggle (Total goals / Goals & times); the preference is
global per-browser and applies to both cards. Removed: Sortable.js CDN,
"Clear scorer history" button from team manager modal, ~700 lines of
editor JS (addScorer/removeScorer/onScorerSelectChange/
onScorerCustomNameChange/onScorerGoalsChange/clearAllScorers/
clearScorerHistory/renderScorersPicker/initSortableOn/
refreshRosterBadges, the `scorers` working array, the
SCORER_CUSTOM_SENTINEL). New: GOALS_BY_GAME map, scorerDisplay state,
loadGoalsFromLocal/refreshGoalsFromGitHub/applyGoalsData,
aggregateGoalsByPlayer (dual-sort: jersey-then-alpha for totals,
earliest-minute for times — three-kind interleaving in times so a
custom or unknown scorer at 12' shows above a roster scorer at 67'),
refreshGoalsLinkStatus. Goals tool game pill color states (the pill
at top of the editor showing the currently-selected game) reworked
from red ("looks like an error") to neutral gray-blue with
green-when-goals-tracked.

Process notes: two bug fixes worth recording for design rationale.
(1) Picker-doesn't-scroll-on-reselect: first pass had
setEntryMinute(getDefaultEntryMinute(), false) in setCurrentGame — the
false meant currentMinute and the active-cell highlight updated but
the strip never actually scrolled. Picking a game with existing goals
lit up cell 67 invisibly off-screen while the visible strip stayed
parked at cell 0. Caught on user feedback ("picker doesn't default to
last goal's minute"); fix was the boolean flip to true. The doScroll
param shows up in several callers and the right value depends on
whether the form is currently visible (boot init = false, view-mode
toggle = false, game-pick = true). (2) .unknown-tag CSS scope catch:
rule was scoped to .goal-row .goal-name .unknown-tag only, then I
used the same class inside .summary-row. Caught on a final grep
before shipping; added the matching .summary-row .summary-name
.unknown-tag rule. Lesson: when adding a class for a shared semantic
marker (here "TBD"), check ALL the surfaces that render with that
class — don't just clone the existing pattern from one parent. Both
phases (Unknown scorer + Final Result) followed a "review-and-comment
first, build second" pattern: user asked an open question, I surfaced
subquestions and tradeoffs (auto-derived vs editable WW score;
just-final vs final+cancelled+un-finalize; always-show vs require-goals;
drift handling Option A vs B), user picked, then I implemented.
Produced cleaner first-pass code than ad-hoc back-and-forth would have
— particularly valuable here because the features spanned HTML + CSS
+ JS + cross-tool coordination + a new write path to a file owned by
another tool. Worth carrying forward for any multi-touch feature.

Final deliverables: GirlsJVSoccerGoals.html (~99KB, new file),
GirlsJVSoccerCard.html (~166KB, modified — was ~157KB pre-session,
+9KB net after removing ~700 lines of picker JS and adding the Goals
integration + sort logic + schedule picker), index.html (~17KB,
4-tile reorder), manifest.json (~2KB, 4 shortcuts), sw.js (~8KB,
CACHE_VERSION='v2'), goals.json (40-byte empty starter
{"version":1,"goalsByGame":{}}). All HTML well-formed (HTMLParser
validation passed), all inline JS parses with node --check. Smoke
tests: 8/8 hold-to-repeat state machine, 7/7 dual-sort with custom
interleaving, 8/8 unknown-scorer aggregation including three-kind
interleaving and bucket collapse, 24/24 Phase 2 schedule-write logic
including 0-0 tie drift edge case and field preservation across
version/layout. Remaining tasks for Rob: deploy the 6 files to repo
root (Card, Goals, index, manifest, sw, goals.json), commit, push to
main, confirm sw.js v2 is what gets served so existing PWA installs
evict their v1 cache and pick up the new Goals.html. First device to
open the Goals tool after deploy runs the migration once (expands
legacy scorersByGame → goal rows + cleans teams.json) and toasts the
result.]



=== Journal Entry 2026-05-08T23:55:00.000000+00:00 ===
2026-05-08-23-55-00-ww-soccer-schedule-venue-and-card-populate.txt
[Long follow-up session on the deployed WW Girls JV Soccer tools.
Eight requested fixes shipped across all five HTML files plus a
follow-up bug fix (dropdown vs/at) and a placement pivot on the
Goals tool's photo credit pill. Net result: schedule entries now
carry venue + address; selecting a game in the Card tool's
dropdown auto-populates Home/Away across all three card modes
(Final, Halftime, Preview) plus kickoff time + venue + address;
Goals + index gained the photo credit pill that Card/Schedule/
Roster already had; back-link wording is consistent across all
four tools.

Process note up front: this is the first session that worked
from a `git clone` of the live repo rather than user-uploaded
files. Rob's "I just linked the github repo" preamble was the
cue — checked /mnt/user-data/uploads (empty), then ran
`git clone --depth 1 https://github.com/rlandquist/WW-Girls-JV-Soccer.git`
from the container; github.com is in the network allowlist and
the public repo cloned without auth. Worked from the cloned tree
throughout, edited in place with str_replace, validated with
HTMLParser + node --check, copied results to /mnt/user-data/outputs/
at the end of each round. This is faster and more reliable than
the upload-per-file pattern prior sessions used; the file-retrieval-
via-create-file gotcha flagged in the May 2 journal entry doesn't
apply when the source of truth is a real git checkout. Worth using
as the default approach going forward whenever the repo is public.

Schema decision flagged early: schedule.json's existing `location`
field stores 'home'/'away'. Rob's request 7 was "add fields for
the location and address" — same word, conflicting concept. Three
options surfaced: (a) add `venue` + `address` keeping existing
`location` as-is; (b) rename existing `location` -> `homeAway`
then use `location` + `address` for the new fields (matches the
user's mental model but requires a small migration); (c) other.
Rob picked (a) in one word ("go with the venue and address option").
All work below assumes that schema — existing `location` field
unchanged and treated as 'home'/'away', new optional `venue`
(place name) and `address` (street) fields added.

Round 1 — surgical, low-risk:

Photo credit pill on Goals + index. The May 2 journal claimed the
credit landed on all four pages, but the live repo showed
otherwise — Goals had no pill, and index still had the original
single-line `<p class="foot">Waukesha West Wolverines</p>`. Either
reverted at some point or never committed. Re-added: Goals got the
same fixed-bottom-center markup Card/Schedule/Roster use, with
full CSS. Index got the document-flow variant per the prior
journal's design intent — structured `<footer class="site-footer">`
with the tagline above the pill, both centered. The journal's
reasoning that "index has no editor/card horizontal split, so a
fixed-position pill would feel out of place" still holds.

Back-link wording: Card / Schedule / Roster's `<- Main` (with
title="Back to main page") changed to `<- Tools` (title="Back to
tools") to match what the Goals tool already had. One-line edit
per file. Roster's mixed CRLF+LF endings were preserved per the
existing pattern.

Round 2 — schedule schema + tool:

schedule.json schema additively gains optional `venue` and
`address` per game. No version bump (purely additive, missing
fields default to ''). The existing 14 games keep working;
fields populate as Rob fills them in via the Schedule tool. The
on-disk schedule.json itself isn't shipped — it'll get the new
fields on its first save through the updated tool.

Schedule tool got two new full-width inputs in the edit form
(`f-venue` + `f-address`), placed below the score/time row.
saveEdit() reads them via getElementById and assigns to g.venue
/ g.address; addGame() defaults both to ''. The existing
"Location" toggle (the home/away picker) was left untouched —
renaming the section label to "Home / Away" would help the
semantic conflict but risks confusing the user mid-season; can
land in a later cleanup pass.

Round 3 — Card tool integration (the big one):

`onCardScheduleSelect` extended substantially. Previously it
read date+opp from the dropdown option's dataset and wrote them
into the manual #game-date / #opp-name inputs, then fired
opponent-logo matching + onGameContextChanged + update. Now it
also looks up the FULL schedule entry from in-memory
SCHEDULE_GAMES (matching by date + opp lowercase, the same key
the dropdown options use) and populates from that record:

  - Home/Away across all three card modes via the existing
    setters (which update both state and toggle-button active
    classes): setWW for the Final card's wwIsHome,
    setHalftimeSide for the Halftime card's wwIsLeft (home ->
    right slot, away -> left slot), setPreviewPrefix for the
    Preview card's VS./AT divider. Default is home when
    `location` is missing or anything other than 'away'.
  - Preview kickoff time. Parses "4:30 PM" -> hhmm + AM/PM via
    a small regex (^(\d{1,2}(?::\d{2})?)\s*(AM|PM|am|pm)?\s*$),
    falls through to setGameAmPm + syncGameTime. Non-numeric
    strings ("TBD") drop into hhmm whole and pass through the
    existing _composedTime helper. Empty just clears.
  - Preview's existing #game-location input gets the venue
    string. New #game-address input added below it (modeled
    after the existing Location field-group with matching
    section-label + hint), gets the address.

All populated values remain editable; the user can override any
field after picking a game. Three calls to update() fire as a
side effect of the setters — wasteful in theory but each one
only re-renders the active card mode, so the cost is negligible.

Preview's VS/AT button labels renamed to "Home (vs)" and "Away
(at)" per request 5. setPreviewPrefix still takes 'VS.'/'AT' as
values, the on-card #vs-text element still renders 'VS.' or 'AT',
and the previewPrefix global still tracks those — only the button
face strings changed. This makes the toggle's semantic meaning
(this is a Home/Away selector that controls the visual prefix)
match its label.

New on-card address line: a `gametime-address` div added below
the existing `gametime-location` line in the preview card markup.
CSS modeled after gametime-location but smaller and subtler
(11px vs 15px, lighter color, normal letter-spacing instead of
the tracked-out caps treatment for location). Hide-when-blank
like location and notes. updatePreview() reads #game-address.value
and toggles display the same way location does.

Bug fix surfaced after Round 3 deployed: the dropdown labels in
both Card and Goals tools hardcoded ' vs ' as the date-opponent
separator regardless of `location`. Result: every option in the
dropdown read "Apr 28 vs Arrowhead" even for away games. Fixed
in three places using the same convention `(g.location === 'away')
? ' at ' : ' vs '`:
  - Card tool's populateCardScheduleDropdown
  - Goals tool's populateScheduleDropdown
  - Goals tool's currently-selected game pill — looks up via
    findScheduleEntry(currentScheduleEntryId) since the pill
    needs the full game record (currentGameDate / currentGameOpp
    don't carry location). Manual-mode games (no schedule entry)
    keep ' vs ' since there's no location info to draw from.

Goals credit pill placement pivot: Rob noted the fixed-bottom-
center pill was too prominent on the Goals tool's compact mobile-
first layout, asked for it at the bottom of the page like on the
index. Switched the .photo-credit rule to document-flow (removed
position:fixed/bottom/left/transform/z-index, added
margin:24px auto 8px, width:max-content, max-width:calc(100%-16px)
so it doesn't stretch full-width). Moved the markup inside
.container so it's the last child after the foot link. Reduced
.container's padding-bottom from 96px to 16px (the 96px was
clearance for a floating pill that's no longer floating); kept
env(safe-area-inset-bottom) handling for iOS PWA home indicator.
Card / Schedule / Roster still use fixed-bottom-center — left
unchanged at Rob's implicit go-ahead (he asked specifically about
Goals); easy to flip those later for consistency if wanted.

Final deliverables (sizes after all rounds + the follow-ups, all
in /mnt/user-data/outputs/):
  GirlsJVSoccerCard.html      ~177KB  (+5.5KB; biggest changes —
                                       onCardScheduleSelect
                                       extension, Address input,
                                       gametime-address line,
                                       VS/AT label rename, dropdown
                                       at/vs fix)
  GirlsJVSoccerSchedule.html   ~67KB  (+0.8KB; venue + address
                                       inputs in edit form,
                                       saveEdit + addGame wiring)
  GirlsJVSoccerRoster.html     ~70KB  (~same; back-link rename
                                       only)
  GirlsJVSoccerGoals.html     ~108KB  (+1.7KB; credit pill add,
                                       dropdown at/vs fix, pill
                                       pill -> document-flow pivot)
  index.html                   ~18KB  (+1.5KB; site-footer with
                                       credit pill in document
                                       flow)

Validation: HTMLParser passed all five files end-of-each-round.
Inline JS extracted and node --check'd on Card / Schedule / Goals
each round. Line endings preserved per file (Card / Schedule:
CRLF, Roster: mixed CRLF+LF, Goals / index: LF). git diff --stat
on the cloned repo confirmed only the expected files were touched
(5 files, ~210 insertions, ~11 deletions before the follow-ups).

Remaining tasks for Rob:
  - Drop the five files into the repo, commit, push.
  - Open the Schedule tool, edit each upcoming game (Catholic
    Memorial 5/11, Muskego 5/12, Elkhorn 5/14, etc.) and fill
    in venue + address. Past games can be left blank — those
    fields don't render where they aren't set.
  - Optionally bump CACHE_VERSION in sw.js from current value
    to the next number if force-refresh on already-installed
    PWA devices is wanted. Without a bump, the changes pick up
    via stale-while-revalidate on next reload — fine for non-
    urgent rollouts.
  - If the same document-flow treatment is wanted on Card /
    Schedule / Roster credit pills (for consistency with Goals
    + index), follow-up session can flip the three .photo-credit
    rules + move the markup into the editor sidebar's flex column.

Conversation-flow notes worth recording for next session:
  - The eight-item batch was efficient. Rob laid them out
    upfront, I planned three rounds + flagged the schema
    question, he confirmed the schema choice in one word,
    I executed all eight and shipped. Less back-and-forth
    than May 2's several-iterations-on-the-photo-credit
    pattern. When the user provides a numbered list, treat it
    as a contract and execute against it; surface only the
    decisions that genuinely block progress.
  - The dropdown vs/at bug was caught by Rob testing the
    deployed version — I'd shipped the broader populate work
    without realizing the dropdown LABELS still read 'vs'
    everywhere. Lesson: when adding home/away awareness to a
    tool, audit ALL strings rendered to the user that imply
    home/away, not just the inputs/state I was actively
    touching. A simple `grep "' vs '\|' at '"` across the
    affected files would have caught it pre-ship.
  - Repo clone was a first for this project; cleaner than
    file-by-file uploads. Default to it next time, and only
    fall back to asking for uploads if the clone fails or
    the repo is private.]

=== Journal Entry 2026-05-08T19:30:00.000000+00:00 ===
2026-05-08-19-30-00-ww-soccer-roster-positions-toggle.txt
[Short follow-up session on the WW Girls JV Soccer tools (already-deployed
GitHub-API-canonical architecture). Added a Show/Hide toggle for the Position
column on the roster card in GirlsJVSoccerRoster.html. New editor field-group
("Position column") with Show/Hide buttons sits directly under the existing
"List layout" group, mirroring its toggle-row pattern. New SHOW_POSITIONS
boolean state, loadShowPositions() loader (defaults to true so existing
rosters are unchanged), setShowPositions() setter, and updatePositionsToggle()
button-state helper — all patterned exactly on the existing LAYOUT plumbing.
Persistence wired through the full chain: localStorage payload (saveRoster +
the LS write inside applyRosterData), GitHub roster.json payload
(saveRosterToGitHub), JSON export/import payload, and applyRosterData
adoption from incoming payloads. Rendering uses a new `.no-pos` CSS class
on the `.rows-block` container: it overrides grid-template-columns to drop
the 56px Position track and hides the `.ppos-card` row cells and `.phead-pos`
header cell. Works for both single- and double-column list layouts via
descendant selectors — no per-cell changes needed in renderPlayerRow or the
headerHTML helpers; the cells are still emitted, just hidden when the class
is on. Single rowsClass variable computed once at the top of renderCardContent
combines the `double` and `no-pos` flags as needed. File grew 1879 → 1959
lines (+80). No other files touched.]

=== Journal Entry 2026-05-04T00:00:00.000000+00:00 ===
2026-05-04-ww-soccer-goals-tool-extraction.txt
[RECONSTRUCTED ENTRY — written 2026-05-08 to fill a journal gap. Date
is approximate (sometime between 2026-05-02 photo-credit session and
2026-05-08); content is reconstructed from in-source comments
("v2 (May 2026)" markers in GirlsJVSoccerCard.html and sw.js), the
shipped GirlsJVSoccerGoals.html file itself, manifest.json's shortcuts
list, and sw.js's APP_SHELL_URLS. If the original session log resurfaces
with conflicting detail, prefer it over this reconstruction.

NEW TOOL: GirlsJVSoccerGoals.html (the "Goals tool"). Per-goal tracking
with date/opponent context — extracted out of the Card tool's
scorers-picker UI so live game-day entry isn't gated behind opening a
desktop-shaped editor. Owns a new file goals.json (read-write). Reads
roster.json (player picker) and schedule.json (game dropdown). The
Card tool is now read-only with respect to scorer data — its picker UI
was removed and replaced with a "Goals tool" link block + a display-
mode toggle (totals vs times).

Game key shape: 'YYYY-MM-DD|opponent-lowercased' — matches the Card
tool's old scorersByGame convention exactly, so legacy data carries
over 1:1 during migration.

Goal entry shape:
  { id, kind: 'roster'|'custom'|'unknown',
    playerId, customName, minute, half }
  - minute is null when unknown (e.g. migrated from old totals-only
    data, or live entry where the tracker missed the time)
  - half is 1 or 2 (mandatory; no extra-time bucket in v1)
  - 'unknown' kind is for when the tracker saw a goal but isn't sure
    who scored — entry is still recorded so the game total stays
    accurate, attribution can be filled in later via the edit form.

Migration + cleanup (maybeMigrateFromTeams in Goals tool):
  Step 1 — Migration (one-shot): reads scorersByGame from teams.json.
  Each old entry was { kind, playerId, customName, goals: N } where
  goals is a count. Expands each into N goal entries with minute=null
  and half=1 (we don't know which half retroactively). Only runs when
  goals.json is empty — subsequent loads see populated GOALS_BY_GAME
  and skip expansion.
  Step 2 — Cleanup (every load when needed): if teams.json still has a
  non-empty scorersByGame field, save a cleaned copy back to GitHub
  without that field. Preserves teams[], spineText, footerModeByGame,
  halftimeModeByGame, conferences, version. Runs even when migration
  is skipped — so device B picks up the cleaned teams.json on next
  load. Card tool no longer writes scorersByGame as of v2, so once
  cleaned the field stays gone.

Goals-tool design — mobile-first, distinct from the desktop-shaped
Card/Schedule/Roster:
  - .app-header sticky at top, navy background, paw + "Goal Tracker"
    title + "← Tools" back-link
  - Deep-red bar (#8a2a2a) under the header is this tool's color
    marker (parallels the colored tile strip on the index page)
  - .container max-width 540px, 16px+ font sizes on inputs (avoids
    iOS auto-zoom-on-focus), 44px min-height tap targets, safe-area
    insets (env(safe-area-inset-top) and -bottom) for iOS PWA
  - Game picker has schedule mode (dropdown of games sorted desc by
    date) and manual mode (free date + opp inputs). Pill at the top
    shows current selection with goal count; flips green when goals
    are tracked
  - Goals list grouped by half, sorted by minute asc, nulls last.
    Row layout: [min']  [#NN NAME]  [✎][✕]
  - Add Goal form: minute input + half toggle + scorer dropdown +
    custom-name input (shown when "Custom name…" picked) + Unknown
    option. After save, form resets but keeps the half (sticky) and
    pre-fills the next minute to lastEnteredMinute.
  - Final Result section: 5 rendered states based on schedule entry.
    Lets the coach mark a scheduled game final right from Goals —
    enter opp's score (WW score is auto-derived from goals tracked,
    not editable), tap "Mark as Final". Save flow loads FRESH
    schedule.json so concurrent edits aren't clobbered, modifies only
    the matched entry's status/scoreUs/scoreThem fields, saves back.
    On conflict-reload, re-applies the change against merged data.
    Un-finalize sets status back to 'scheduled' but preserves
    scoreThem (per design: an accidental un-finalize shouldn't erase
    the typed score).
  - Drift hint: when goals tracked diverge from saved scoreUs on a
    final game, a small italic warning appears.

Card tool changes (15+ surgical edits to GirlsJVSoccerCard.html):
  - Removed the entire scorers-picker UI (renderScorersPicker, the
    drag-to-reorder Sortable.js wiring from session 11, the per-row
    inputs, the Clear-all button, the roster badge that lived in the
    picker header).
  - Replaced with .goals-link-block: navy-tinted bg, "Scorers are
    managed in the Goals tool" copy, a deep-red "Open Goals tool ↗"
    button (target="_blank" rel="noopener"), and a status line
    showing "N scorers loaded" / "No scorers yet" / "Loading…".
  - Display-mode toggle preserved (totals vs times — global per-
    browser pref, not per-game).
  - GOALS_BY_GAME global added; refreshGoalsFromGitHub() loads
    goals.json on startup. setLogoOppScore/Halftime now read from
    GOALS_BY_GAME[currentGameKey] instead of from teams.json's
    scorersByGame.
  - persistGameStateNow() simplified — only footerModeByGame and
    halftimeModeByGame still get written (those are card-tool-owned
    settings); SCORER_HISTORY no longer touched on save (left
    initialized so legacy data survives until Goals tool migrates).
  - Card-tool schedule dropdown added (mirrors Goals'): coaches now
    pick a scheduled game from a dropdown to populate date+opp
    instead of typing them manually, so the gameKey stays consistent
    between tools without trailing-whitespace gotchas. Manual entry
    still works.

Index page (index.html): added a 4th tile for Goals with the deep-red
accent (--tool-red: #8a2a2a). Tile order is Card / Goals / Schedule /
Roster — Goals slots in second so it pairs with Card visually (track
during, build after). Grid breakpoints adjusted: 1-up mobile → 2-up
tablet (560px) → 4-up desktop (900px), up from 1-up → 2-up → 3-up.

Manifest (manifest.json): added a Goals shortcut. Now 4 jump-shortcuts
total. Description updated to "build cards, manage schedule, edit
roster, track goals". Maskable + any icons unchanged.

Service worker (sw.js): CACHE_VERSION bumped 'v1' → 'v2'. APP_SHELL_URLS
gained './GirlsJVSoccerGoals.html'. Comment header updated to mention
"five pages (index + four tools)" and document the v2 reason. The
v1 → v2 bump means devices with a v1 cache evict and re-prime the shell
on next load — without it, already-cached devices wouldn't see the
new Goals page until a hard reload.

What did NOT change in this release:
  - common.js untouched (Goals tool uses the existing WWCommon API as-is)
  - Schedule and Roster tools untouched
  - teams.json schema unchanged (scorersByGame is still parsed for
    migration, just no longer written)
  - The photo-credit pill from the 2026-05-02 session was NOT added
    to the Goals tool (different mobile-first layout — no editor
    sidebar to anchor it, and a fixed-position pill would compete
    with the bottom safe-area inset). Decision: leave Goals
    credit-less for now, revisit if Rob wants it.

Known gap left for future sessions:
  - Card tool's Goals-tool link uses target="_blank", which inside an
    iOS PWA opens Safari rather than another standalone window —
    breaks the installed-app feel. Fix: drop target/rel, or detect
    display-mode: standalone and conditionally remove. Filed as
    backlog, not blocking.
  - No cross-tab live updates: if Goals saves while Card is open in
    another tab, Card doesn't see the new goal until reload. A
    BroadcastChannel('ww-soccer-sync') ping in common.js's saveJson
    would solve this for ~5 lines of code. Filed for next session.
  - No documentation update accompanied this release — README still
    described "three tools," lacked a Goals row in the file table,
    and had no goals.json schema. (Closed in 2026-05-08 docs session.)]


=== Journal Entry 2026-05-08T00:00:00.000000+00:00 ===
2026-05-08-ww-soccer-docs-catchup.txt
[Documentation-only session. No code changes. Reviewed full repo state
against the journal and found significant doc drift — README and
journal had not caught up with the May 2026 Goals-tool extraction or
the 2026-05-02 conferences + photo-credit + back-link work in the way
Rob's "reproducible from journal alone" goal requires.

Inconsistencies inventoried (full review delivered to Rob in chat):
  1. README opens with "Three social-media tools" — should be four.
  2. README's bullet list, ASCII repo tree, file inventory table, and
     setup walkthroughs all referenced 3 tools / 4 pages. Real state:
     4 tools / 5 pages.
  3. README's teams.json schema example still listed scorersByGame as
     a card-specific field. Reality: scorersByGame is migrated OUT by
     the Goals tool on first load and never written by the Card tool
     anymore. teams.json schema also missing the conferences map and
     per-team conference field added 2026-05-02.
  4. No goals.json schema documented at all.
  5. README's roster.json schema showed players with a single name
     field. Real shape is firstName + lastName (legacy name string
     still loads via normalizePlayer's "Last, First" splitter).
  6. README's CACHE_VERSION example showed 'v1' — sw.js is at 'v2'.
  7. README's Logos table didn't list Classic8.png or
     RobertLandquistPhotography.png even though all four pages
     reference the latter and the Card+Schedule render the former as
     conference badges.
  8. Android shortcut count described as "three" — manifest.json
     ships four (Card, Goals, Schedule, Roster).
  9. Journal had no entry for the Goals-tool extraction — a
     substantial release referenced in source comments
     ("v2 (May 2026): Scorer entry was extracted into a separate
     tool" in Card; "v2 (May 2026): Added GirlsJVSoccerGoals.html
     to the shell" in sw.js) but no session entry summarizing it.
 10. Back-link wording inconsistent: Card/Schedule/Roster use
     "← Main"; Goals uses "← Tools". Both link to ./. Cosmetic only,
     not fixed in this session.

Files changed:
  - README.md — full rewrite to reflect current 4-tool / 5-page
    state. Added: Goals tool to opening list and bullet list, Goals
    row to file inventory, goals.json + Classic8.png +
    RobertLandquistPhotography.png to Logos table, "Who reads/writes
    what" matrix, Game-day workflow narrative, Custom-typed
    opponents subsection, Conference badges subsection, "Goals I
    entered don't show up on the Card" troubleshooting entry,
    goals.json schema block, updated teams.json schema (conferences
    map + per-team conference field, scorersByGame removed with a
    migration note), updated roster.json schema (firstName/lastName
    + legacy fallback note), CACHE_VERSION example bumped to v2 with
    a "current version: v2" callout. Repo-tree ASCII updated. Setup
    walkthrough updated to "five pages."
  - journal.txt — appended two entries: the reconstructed
    Goals-tool extraction entry (date 2026-05-04, explicitly flagged
    as reconstructed from source-comment landmarks), and this entry
    documenting the docs-catchup session itself.

Files NOT changed in this session (code/data left alone):
  - All four tool HTMLs, common.js, sw.js, manifest.json, all JSON
    data files, all logo files. This was a docs-only pass.

Backlog flagged but deferred (Rob to prioritize):
  - Goals-tool back-link wording ("← Tools" → "← Main") for
    consistency with the other three tools.
  - Goals-tool photo-credit pill — decide whether the credit
    convention applies to the mobile-first layout, or document the
    exception explicitly.
  - target="_blank" on Card → Goals links breaks PWA standalone mode
    on iOS; either drop or feature-detect.
  - BroadcastChannel('ww-soccer-sync') in common.js for cross-tab
    live updates.
  - Conflict-aware saveJson — surface a "the file was changed
    elsewhere; reload?" toast on 409, instead of silent failure or
    silent overwrite.
  - "Last synced" timestamp on the GH config panel pill (the code
    already maintains GH_SYNC_TIMES_KEY; just needs UI wiring).
  - Roster.html line-ending normalization (still mixed CRLF+LF, 121
    bare LFs alongside ~1700 CRLFs per the 2026-05-02 entry); commit
    a whitespace-only normalization so future diffs stay clean.
  - Top-of-file build-date comment on each HTML (Session 10 backlog
    item, still open).
  - True 192/512 maskable PNG icons for the manifest (existing TODO).

Process note: this is the second time documentation has lagged code
in this project — the first was 2026-04-26 when the README was
written in the same final session that shipped the architecture
migration; the second was the Goals-tool release in early May 2026.
Pattern worth flagging: any session that ships a new top-level page
or changes a JSON schema should treat the README + journal entry as
deliverables, not as a follow-up. Future-session checklist proposal:
before declaring a release "shipped," re-grep the README for the old
file count / schema field / shortcut list and update in place.]
